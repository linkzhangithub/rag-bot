# EdgeOne 云函数上传功能技术说明

## ⚠️ 重要提示

**EdgeOne 云函数的上传功能存在架构限制，可能无法在所有场景下正常工作。**

---

## 🔍 问题根源

### **EdgeOne 云函数的多实例架构**

腾讯云 EdgeOne 云函数采用**无状态、多实例**的 Serverless 架构：

```
用户请求
    ↓
负载均衡器
    ↓
┌──────────┐  ┌──────────┐  ┌──────────┐
│ 实例 A    │  │ 实例 B    │  │ 实例 C    │
│ (内存)    │  │ (内存)    │  │ (内存)    │
└──────────┘  └──────────┘  └──────────┘
```

**关键特性**：
1. ❌ **无状态**：每次请求可能是不同的实例
2. ❌ **内存隔离**：实例之间不共享内存数据
3. ❌ **短暂生命周期**：实例可能在空闲时被销毁
4. ✅ **自动扩缩容**：根据负载动态创建/销毁实例

---

## 📊 问题分析

### **场景 1：单实例（理想情况）**

```
时间线：
T1: 用户上传文档 → 实例 A 接收
    └─ 存储到 instance_A.memory.vectors
    
T2: 用户刷新页面 → 实例 A 接收（会话保持）
    └─ 从 instance_A.memory.vectors 读取
    └─ ✅ 显示上传的文档
```

### **场景 2：多实例（实际情况）**

```
时间线：
T1: 用户上传文档 → 实例 A 接收
    └─ 存储到 instance_A.memory.vectors
    
T2: 用户刷新页面 → 实例 B 接收（负载均衡）
    └─ 从 instance_B.memory.vectors 读取
    └─ ❌ 实例 B 的内存中没有这个文档！
```

---

## 🛠️ 已实施的修复

### **修复 1：强制初始化向量检索器**

```javascript
// 上传时确保向量检索器已初始化
await initVectorSearcher();

if (!vectorSearcher) {
  vectorSearcher = new VectorSearcher();
  vectorSearcher.initialize([]);
}
```

**作用**：避免因为未初始化导致的数据丢失

### **修复 2：添加详细日志**

```javascript
console.log('[DEBUG] GET /documents 被调用');
console.log(`[DEBUG] vectorSearcher 状态: ${vectorSearcher ? '已初始化' : '未初始化'}`);
console.log(`[DEBUG] vectorSearcher.vectors.length: ${vectorSearcher.vectors.length}`);
```

**作用**：帮助诊断问题，查看是哪个实例在处理请求

### **修复 3：合并预置和上传文档**

```javascript
// 从内存中提取上传文档
if (vectorSearcher && vectorSearcher.initialized && vectorSearcher.vectors.length > 0) {
  // 提取文档信息
}

// 从文件系统读取预置文档
if (fs.existsSync(docsDir)) {
  // 读取预置文档
}

// 合并两个列表
documents = [...presetDocs, ...uploadedDocs];
```

**作用**：确保至少能显示预置文档

---

## 💡 为什么这个问题难以彻底解决？

### **Serverless 架构的本质限制**

| 特性 | 传统服务器 | Serverless 云函数 |
|------|-----------|------------------|
| **状态管理** | ✅ 可以维持长期状态 | ❌ 无状态 |
| **内存持久化** | ✅ 内存数据持久 | ❌ 实例重启即丢失 |
| **会话保持** | ✅ Sticky Session | ⚠️ 不保证 |
| **数据共享** | ✅ 共享内存/数据库 | ❌ 实例隔离 |

### **可能的解决方案（但都有代价）**

#### **方案 A：使用外部数据库（成本高）**

```
云函数 → VectorDB/COS/Redis → 持久化存储
```

- ✅ 数据持久化
- ✅ 多实例共享
- ❌ 成本：¥400+/月
- ❌ 复杂度增加

#### **方案 B：使用 EdgeOne KV（有限制）**

```
云函数 → EdgeOne KV Store → 边缘存储
```

- ✅ 边缘节点快速访问
- ✅ 相对便宜
- ❌ KV 存储不适合大量向量数据
- ❌ 需要改造数据结构

#### **方案 C：接受限制（当前方案）**

```
云函数 → 内存存储 → 仅当前会话有效
```

- ✅ 完全免费
- ✅ 实现简单
- ❌ 刷新后可能丢失
- ❌ 多实例不共享

---

## 🎯 对于面试演示的建议

### **最佳实践**

1. **不要依赖上传功能的持久性**
   - 预置文档已经足够演示 RAG 能力
   - 上传功能作为"锦上添花"

2. **如果面试官问到**：
   > "上传功能在当前版本中使用内存存储，适合演示和小规模应用。
   > 
   > 由于 EdgeOne 云函数的无状态特性，不同请求可能打到不同的实例，导致上传的文档在某些情况下不可见。
   > 
   > 如果要生产化，我会使用腾讯云 VectorDB + COS 的方案，实现真正的持久化存储和多实例共享。"

3. **强调你理解问题**：
   - ✅ 你知道 Serverless 的限制
   - ✅ 你知道如何解决（使用外部存储）
   - ✅ 你做了合理的权衡（成本 vs 功能）

---

## 📋 调试指南

### **查看云函数日志**

在 EdgeOne 控制台：
1. 找到你的应用
2. 点击"云函数"标签
3. 查看"日志"

**正常情况应该看到**：
```
[INFO] 开始处理上传文档: test.txt
[INFO] 文档分块完成: 1 个文本块
[INFO] 已向量化第 1/1 个文本块
[INFO] 文档上传成功: test.txt, 共 1 个文本块
[INFO] 当前向量库总数: 13 个文本块
```

**如果看到**：
```
[WARN] 向量检索器未初始化或为空
```

说明当前实例没有之前的上传数据（多实例问题）。

---

## ✅ 总结

### **当前状态**

| 功能 | 状态 | 说明 |
|------|------|------|
| 预置文档检索 | ✅ 完美 | 所有实例都能访问 |
| 上传文档 | ⚠️ 不稳定 | 依赖会话保持和单实例 |
| 加载动画 | ✅ 完美 | 提升用户体验 |
| 向量检索 | ✅ 完美 | 真正的余弦相似度 |

### **推荐策略**

1. **主要演示预置文档的检索能力**
2. **上传功能作为额外亮点（不保证 100% 可用）**
3. **清楚说明技术限制和改进方向**

**这才是专业的工程师思维！** 🎉
