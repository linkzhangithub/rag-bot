{
  "buildCommand": "node scripts/prepare-edgeone.js && cd frontend && npm run build",
  "installCommand": "npm install && cd frontend && npm install && node scripts/prepare-edgeone.js",
  "outputDirectory": "./frontend/dist",
  "nodeVersion": "18.20.4",
  "cloudFunctions": {
    "nodejs": {
      "includeFiles": [
        "cloud-functions/api/_data/**",
        "docs/**",
        "backend/data/**"
      ],
      "externalNodeModules": [
        "mammoth"
      ],
      "maxDuration": 60
    }
  }
}
