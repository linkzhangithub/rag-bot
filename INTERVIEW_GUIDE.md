# 面试演示指南

## 📋 部署后验证清单

### 1. 触发重新部署

在 [EdgeOne 控制台](https://console.cloud.tencent.com/edgeone) 中：
1. 找到你的应用
2. 点击"重新部署"或"触发构建"
3. 等待 2-3 分钟直到部署完成

---

## ✅ 功能测试流程

### **测试 1：基础对话功能**

1. 打开网站
2. 在聊天框输入："什么是 RAG？"
3. 观察：
   - ✅ AI 应该流式输出答案
   - ✅ 显示来源文档和匹配度分数
   - ✅ 分数应该是真实的（如 0.85、0.72）

**预期结果**：正常回答，显示相关文档

---

### **测试 2：上传文档功能**

1. 点击侧边栏的"上传文档"按钮
2. 选择一个 TXT 或 MD 文件（建议小文件，< 10KB）
3. 或者手动输入文件名和内容
4. 点击上传

**观察**：
- ✅ 显示上传进度
- ✅ 提示"文档上传成功，共 X 个文本块"
- ✅ 文档列表中出现新文档

**注意**：
- 上传时间取决于文档大小（每 800 字符约需 1 秒）
- 大文件可能需要 5-10 秒

---

### **测试 3：检索新上传的文档**

1. 上传一个包含特定内容的文档（例如："我的名字是张三"）
2. 在聊天框提问："我的名字是什么？"
3. 观察：
   - ✅ AI 应该从新上传的文档中找到答案
   - ✅ 来源显示你刚上传的文档名
   - ✅ 匹配度分数合理

**预期结果**：能够检索到新上传的文档内容

---

### **测试 4：删除文档功能**

1. 在文档列表中点击某个文档的删除按钮
2. 确认删除
3. 观察：
   - ✅ 文档从列表中消失
   - ✅ 提示"已删除文档"

**注意**：只删除内存中的向量，不影响预置文档

---

### **测试 5：多轮对话**

1. 问第一个问题："什么是人工智能？"
2. 等回答完成后，追问："它有哪些应用？"
3. 观察：
   - ⚠️ 由于无状态设计，AI 可能不知道上下文
   - ✅ 但每次问答都能正常工作

**说明**：这是云函数架构的限制，面试时可以解释

---

## 🎯 面试演示脚本

### **开场介绍（30秒）**

> "这是我开发的 RAG 智能文档问答系统。它基于 Retrieval-Augmented Generation 技术，能够实现基于文档的智能问答。"

### **核心功能演示（2分钟）**

1. **展示预置文档**
   > "系统预置了 3 个专业文档，包括 AI 医疗、RAG 技术和脑机接口。"

2. **演示向量检索**
   > "当我提问时，系统会使用向量检索找到最相关的文档片段。"
   > （提问并展示来源分数）
   > "这些分数是真实的余弦相似度，不是随机生成的。"

3. **演示上传功能**
   > "我还可以动态上传文档。让我上传一个测试文件..."
   > （上传一个小文件）
   > "上传后立即可以进行检索，完全不需要额外的数据库服务。"

4. **解释技术实现**
   > "这个系统的亮点是实现了零成本的向量检索：
   > - 预计算文档向量并打包到云函数
   > - 运行时进行真正的余弦相似度计算
   > - 支持动态上传和即时向量化
   > - 完全免费，无需 VectorDB 或其他付费服务"

### **技术深度展示（1分钟）**

如果被问到技术细节：

> "我采用了混合架构：
> - 本地开发使用完整的 RAG 系统，包括向量数据库、会话管理等
> - 生产环境考虑到成本和云函数限制，使用了预计算向量的方案
> - 虽然牺牲了持久化存储，但保留了核心的向量检索能力
> - 如果要升级为生产级，我会使用腾讯云 VectorDB + COS 的方案"

---

## ⚠️ 注意事项

### **不要做的事**

1. ❌ **不要刷新页面**
   - 上传的文档会丢失
   - 面试时保持页面打开

2. ❌ **不要上传大文件**
   - 建议 < 10KB 的文本文件
   - 大文件会导致超时

3. ❌ **不要同时上传多个文档**
   - 一次上传一个，等待完成
   - 避免 API 限流

### **可以做的事**

1. ✅ **准备几个测试问题**
   - 针对预置文档的问题
   - 针对上传文档的问题

2. ✅ **准备技术问题的回答**
   - RAG 的工作原理
   - 为什么选择这种架构
   - 如何优化和改进

3. ✅ **展示代码结构**
   - 如果面试官要求看代码
   - 重点展示 vector-search.js 和云函数实现

---

## 💡 常见问题及回答

### Q1: 为什么上传的文档刷新后就没了？

> "因为云函数是无状态的，数据存储在内存中。如果要持久化，我会使用腾讯云 COS 对象存储和 VectorDB，但这需要额外的成本。对于演示目的，当前的方案已经足够展示技术能力。"

### Q2: 为什么不使用专业的向量数据库？

> "我考虑过 Supabase、Neon 等免费方案，但它们需要跨国网络连接，稳定性不可控。当前的预计算向量方案完全免费，而且性能足够好（响应时间 < 2秒）。对于作品集项目，这是一个合理的权衡。"

### Q3: 这个系统能处理多少文档？

> "当前版本适合小规模应用（< 50 个文档）。如果要扩展到大规模，我会：
> 1. 使用专业的向量数据库（如腾讯云 VectorDB）
> 2. 实现异步任务队列处理向量化
> 3. 添加缓存层提升性能
> 4. 使用 CDN 加速静态资源"

### Q4: 会话历史怎么实现的？

> "本地开发版本使用了内存 Map 存储会话历史，支持 30 分钟 TTL。但云函数版本由于无状态特性，无法维持会话。如果要实现，我会使用 Redis 或 TDSQL 存储会话数据。"

---

## 📊 性能指标参考

| 操作 | 预期时间 | 说明 |
|------|---------|------|
| 首次加载 | 2-3 秒 | 加载向量数据 |
| 普通问答 | 1-2 秒 | 向量检索 + AI 生成 |
| 上传小文档 (< 5KB) | 2-3 秒 | 分块 + 向量化 |
| 上传中文档 (5-20KB) | 5-10 秒 | 取决于文本块数量 |
| 删除文档 | < 1 秒 | 内存操作 |

---

## 🎉 最后检查

在面试前，确保：

- [ ] EdgeOne 部署已完成
- [ ] 网站可以正常访问
- [ ] 预置文档可以正常检索
- [ ] 上传功能工作正常
- [ ] 删除功能工作正常
- [ ] 准备好演示脚本
- [ ] 准备好技术问题的回答

**祝你面试成功！** 🚀
