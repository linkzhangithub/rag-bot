# 架构设计说明

## 📋 概述

本项目实现了两种不同的 RAG 架构，分别适用于开发环境和生产环境。

---

## 🏗️ 架构对比

### 1. 本地开发环境（完整 RAG）

```
┌─────────────────────────────────────────┐
│         Express Server (有状态)          │
├─────────────────────────────────────────┤
│  • 内存向量数据库 (JSON 文件持久化)      │
│  • 会话历史管理 (Map)                    │
│  • 完整的 RAG 流程                       │
└────────────┬────────────────────────────┘
             │
    ┌────────▼────────┐
    │  向量检索引擎     │
    │  • 余弦相似度     │
    │  • 混合检索       │
    │  • Rerank        │
    └─────────────────┘
```

**特点**：
- ✅ 真正的向量检索
- ✅ 支持动态上传/删除文档
- ✅ 会话历史管理
- ✅ 高精度检索

**适用场景**：本地开发、演示、小规模应用

---

### 2. 生产环境 - EdgeOne 云函数（简化版 RAG）

```
┌─────────────────────────────────────────┐
│      EdgeOne Cloud Function (无状态)     │
├─────────────────────────────────────────┤
│  • 只读文件系统 (docs/)                  │
│  • 无持久化存储                          │
│  • 简化的全文检索                        │
└────────────┬────────────────────────────┘
             │
    ┌────────▼────────┐
    │   智谱 AI API    │
    │  • GLM-4-Flash   │
    │  • SSE 流式输出  │
    └─────────────────┘
```

**特点**：
- ⚠️ 全文检索替代向量检索
- ⚠️ 文档预置，不支持动态上传
- ⚠️ 无会话历史
- ✅ 完全免费
- ✅ 易于部署

**适用场景**：作品集展示、小型演示应用

---

## 🤔 为什么选择简化方案？

### 技术限制

1. **云函数无状态**
   - 每次请求都是新实例
   - 无法维持向量数据库
   - 无法保存会话历史

2. **只读文件系统**
   - 只能读取预打包的文件
   - 无法上传新文档
   - 无法写入向量数据

3. **资源限制**
   - 最大运行时间：60 秒
   - 内存限制：512MB
   - 无法安装大型依赖

### 成本考虑

| 方案 | 月成本 | 说明 |
|------|--------|------|
| 当前方案（EdgeOne） | ¥0 | 免费额度内 |
| VectorDB + COS | ¥400+ | 需要付费服务 |
| 传统 CVM | ¥50-100 | 学生优惠后 |

作为作品集项目，**零成本**是重要考量因素。

---

## 🚀 生产环境升级路径

如果要升级为生产级应用，建议：

### 方案 A：腾讯云原生方案（推荐）

```
EdgeOne (前端) → SCF (API) → VectorDB (向量检索)
                              → COS (文档存储)
                              → Redis (会话管理)
```

**优势**：
- 真正的向量检索
- 支持动态文档管理
- 高可用、可扩展

**成本**：约 ¥400-600/月

### 方案 B：混合架构

```
EdgeOne (前端) → 传统 CVM (后端 API + 向量检索)
```

**优势**：
- 保留完整 RAG 功能
- 成本较低（学生机 ¥10/月）

**劣势**：
- 需要维护服务器
- 扩展性较差

---

## 📚 技术亮点

尽管使用了简化方案，项目仍然展示了以下核心技术：

1. **RAG 原理理解**
   - 文档分块策略
   - 上下文构建
   - Prompt 工程

2. **前端工程化**
   - Vue 3 Composition API
   - Pinia 状态管理
   - SSE 流式处理
   - 响应式设计

3. **后端开发**
   - Express 路由设计
   - 错误处理机制
   - 指数退避重试
   - API 限流

4. **部署运维**
   - 腾讯云 EdgeOne 配置
   - 环境变量管理
   - CI/CD 流程

---

## 💡 总结

这个项目的核心价值在于：

1. **展示了完整的 RAG 系统开发能力**（本地版本）
2. **体现了技术选型的权衡思维**（生产版本）
3. **证明了可以在资源受限环境下交付可用产品**

